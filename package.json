{
  "name": "autodidact",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postinstall": "prisma generate --schema=admin/prisma/schema.prisma"
  },
  "prisma": {
    "schema": "admin/prisma/schema.prisma"
  }
}
